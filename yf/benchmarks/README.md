# Benchmarks

`static/m0-smoke.json` is a tiny synthetic fixture authored for adapter and installation checks. It is not experimental evidence and is not part of a frozen evaluation set.

Before adding an external fixture, record its source, version, license, checksum, coordinate units, and every transformation in `Docs/Research/Technical Sources.md` or a dataset-specific note.
