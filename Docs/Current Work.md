# Current Work

## Active milestone

[[M0 - Experiment contract]]

## Current objective

Work through M0 together and decide exactly what experiment we are running before result-producing components can influence the rules.

The reusable development foundation is now present in `yf/`: canonical part/problem/candidate contracts, a pinned Spyrrow adapter, immutable candidate archives, a CLI, and a synthetic smoke fixture. This is enabling infrastructure, not evidence that M0 or M2 has passed.

## Next step

Define the primary outcome and comparison: what “better” means, which costs count, and how the oracle will be compared with the strongest legitimate baseline.

## Foundation completed

- One persistent `yf/` Python project, managed with `uv` and Python 3.12.
- Spyrrow 0.9.0 mapped behind a YieldForge-owned adapter.
- Progressive feasible solutions normalized, fixed-sheet filtered, deduplicated, and archived.
- Native one-second smoke test and CLI path verified.

## Still unresolved

No M0 acceptance decision, economic benchmark result, residual-geometry proof, future-aware policy result, or validated savings claim exists yet.
